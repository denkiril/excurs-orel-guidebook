import { StringTickerPipe } from './string-ticker.pipe';

describe('StringTickerPipe', () => {
  it('create an instance', () => {
    const pipe = new StringTickerPipe();
    expect(pipe).toBeTruthy();
  });
});
